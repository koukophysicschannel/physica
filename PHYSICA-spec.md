# PHYSICA 仕様書 v1

高校物理の学習量を「物理力(PHYSICA値)」として可視化するPWA。
問題を解いてタップすると加点され、時間経過で忘却曲線に従って目減りする。
PHYSICA値は定期テストの予想得点・大学ランクに換算される。

## 技術方針

- PWA(manifest.json + service worker、ホーム画面追加・オフライン動作対応)
- Vanilla JS + HTML + CSS。フレームワーク不使用。ビルド工程なし
- ホスティング: GitHub Pages 想定(静的ファイルのみ)
- データ保存: localStorage のみ(ログインなし)。保存するのは**タップ履歴だけ**
  - 形式: `{ problemId: [timestamp, timestamp, ...] }`(エポックミリ秒)
  - 現在のPHYSICA値は保存せず、表示のたびに履歴から計算する
- スマホ縦画面前提。PCでも崩れない程度でよい

## スコア仕様

### 配点(config.jsonに外出し)

| 種別 | 点 |
|---|---|
| 基礎CHECK(章単位で1タップ) | 1 |
| 基本例題・基本問題 | 2 |
| 応用問題 | 3 |
| 重問A | 5 |
| 重問B | 7 |
| 重問・考察 | 5 |

- タップは同一問題に何度でも可。毎回満額加点(周回累積型)
- 全1周満点: リードα 1,288点 + 重問 845点 = **2,133点**

### 忘却(減衰)

タップ1回ぶんの保持値 = 配点 × R(t)

```
R(t) = 0.3 + 0.7 * exp(-sqrt(t / tau))    tau = 7(日)、下限0.3
```

- t はタップからの経過日数(実数)
- PHYSICA値 = 全タップの保持値の総和
- 表示は小数第4位まで、常時更新し「減り続ける」ことを見せる
- 内部計算は倍精度のまま。表示のみ丸め

### 予想得点(100点換算)

```
予想得点 = 範囲内保持点 / (範囲内満点 × 1.7) × 100    上限100でキャップ
```

- 通常モード: 範囲=全問題
- 定期考査モード: 範囲=ユーザーが選択した章の集合
- 係数1.7は「現実的スケジュールで3周した直後 ≒ 100点」になる較正値

### 大学ランク(config.jsonに外出し、微調整前提)

保持点(全範囲)で判定。ただし**5分野すべて**が当該ランク相当の水準を
超えたときにそのランクと認定(分野別予想得点の最小値で判定する)。

```json
"ranks": [
  { "label": "東大・京大", "score": 3600 },
  { "label": "早慶・旧帝", "score": 2900 },
  { "label": "MARCH・地方国立", "score": 2200 },
  { "label": "日東駒専・地方私立", "score": 1450 }
]
```

## config.json(全パラメータ集約)

```json
{
  "decay": { "tau": 7, "floor": 0.3 },
  "conversion": { "k": 1.7, "cap": 100 },
  "points": { "基礎CHECK": 1, "基本例題": 2, "基本問題": 2, "応用問題": 3, "重問A": 5, "重問B": 7, "考察": 5 },
  "ranks": [ ...上記... ],
  "displayDecimals": 4
}
```

コード本体にマジックナンバーを書かない。すべてここから読む。

## 問題データ

同梱の2つのCSVから問題マスタ(problems.json)をビルドスクリプトで生成する。
(スクリプトはPython可。生成物のみをアプリが読む)

### リードα: titles-all-mapping.csv

- `旧タイトル` 列から `(\d+)章_(基礎CHECK|基本例題|基本問題|応用問題)(\d*)` を抽出
- 基礎CHECKは章単位1件(問題番号なし)として扱う
- 27章ぶん、計591件(基礎CHECK27 + 基本例題93 + 基本問題338 + 応用問題133)
- 章別満点(検算用): 1章57 / 2章62 / 3章51 / 4章77 / 5章38 / 6章67 / 7章54 /
  8章66 / 9章53 / 10章40 / 11章52 / 12章33 / 13章60 / 14章42 / 15章18 /
  16章22 / 17章55 / 18章29 / 19章53 / 20章28 / 21章55 / 22章56 / 23章36 /
  24章56 / 25章47 / 26章42 / 27章39、合計1,288

### 重問: juyomon-mapping.csv

- `num`(問題番号)、`分野`、`難易度`(A/B/−)を使用
- 難易度「−」は考察問題として5点
- 163問(A143 / B15 / 考察5)、満点845

### 章→分野マッピング(要確認。仮置きでビルドし、定数として容易に修正可能にする)

- 1〜10章: 力学 / 11〜14章: 熱 / 15〜19章: 波動 / 20〜26章: 電磁気 / 27章: 原子

重問は `分野` 列をそのまま使用(考察は分野バランス計算から除外し総点のみ計上)。

## 画面

### 1. ホーム

- PHYSICA値(小数第4位、等幅フォント、常時減少アニメーション)を最上部に大きく
- 全範囲の予想得点(整数)
- 大学ランクゲージ(現在ランク、次ランクまでの残り点)
- 5分野レーダーチャート(分野別予想得点)
- 定期考査カード(設定時のみ: 範囲、予想得点、試験日カウントダウン)

### 2. 問題リスト

- リードα(章ごと) / 重問(分野ごと)の2タブ
- 問題はタイル表示。**保持率をそのまま背景色の濃淡にする**(解きたて=濃、
  忘却で白へ、未着手=無色)。ヒートマップとして「白い所=やるべき所」が見える
- タイルタップで加点。ポップするフィードバック(scale + 色変化)
- タイル長押しで誤タップ取り消し(直近タップ1件を削除)

### 3. 定期考査モード設定

- 試験日(日付ピッカー)と範囲(章の複数選択)を設定
- 設定するとホームに定期考査カードが出る

### 4. 設定

- データのエクスポート/インポート(タップ履歴JSONのダウンロード/読込。
  機種変更・ブラウザデータ消失への自衛手段)
- リセット(確認2段階)

## デザイン指針

- フラット・余白広め・カード角丸12px。グラデーション/影は使わない
- タイル濃淡は緑系1色のランプ(保持率100%=濃緑 → 30%=淡緑 → 未着手=白/枠のみ)
- 数字は等幅フォント。減り続けるPHYSICA値が画面の主役
- 絵文字は使わない

## 受け入れ基準

1. タップ→加点→リロードしても履歴から正しく再計算される
2. 端末の時計で時間が進むとPHYSICA値が式どおり減る(タイムスタンプを過去に
   偽装したテストデータで検証)
3. 全問1周直後のPHYSICA値が2,133±誤差、予想得点の式が仕様どおり
4. 定期考査モードで範囲満点が章別合計と一致
5. オフラインで起動・タップ・表示更新が動く
6. config.jsonの数値変更だけでランク・減衰・配点が変わる

## やらないこと(v1スコープ外)

- ログイン、サーバー保存、課金、ランキング、通知
