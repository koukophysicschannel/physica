# PHYSICA v2.4 開発指示書

v2.3(edition・匿名ID・7学年、commit 3a018b8)は反映確認済み。今回は以下4件。
完了したらsw.jsバージョンを上げて本番反映・検品まで。

## 1. 同日タップ制限

- 同一問題は同一日に1回しか加点できない(「同一日」=端末ローカル時刻の
  0時切り替わり)
- 同日2回目のタップ: 加点せず、タイルを短く揺らして
  「今日はもう解きました。また明日」とトースト表示
- 長押しで直近タップを取り消した場合、その日の再タップは可能
- config.jsonに "dailyLimit": true を追加してオン・オフ可能に
- about.htmlの仕様説明に「同じ問題は1日1回まで加点。翌日になると
  再度加点できる」を追記

検品: タイムスタンプ偽装データで3ケース確認
(同日拒否 / 翌日許可 / 取り消し後の同日再タップ許可)

## 2. ホームの「予想得点(全範囲)」を削除

- ホーム画面のPHYSICA値の下にある「予想得点(全範囲)」表示を削除。
  PHYSICA値のみを主役にする
- 目標カード内の予想得点・仕上がり度は変更しない

## 3. 長押し取り消しの案内

- ウィザード最終ステップ(問題を1つタップしてみよう)に
  「間違えてタップしたら、長押しで取り消せます」の1行を追加

## 4. 「このアプリについて」ページ(mission.html)の器

- ナビまたは設定画面から開ける mission.html を新規作成
- 中身は後日管理者が書くテキストに差し替えるため、仮見出しで
  プレースホルダのみ:
  - なぜ作ったか
  - 使い方の心得
  - データについて(先生には見えない・自分のためだけの記録)
- about.html(仕様説明)とは別ページとして共存させる

## 検品

- ローカル: 上記各項目
- 本番: 全アセット200・SW更新・オフライン起動・375px幅スクリーンショット
- 報告にはsw.jsの新バージョンとcommit ID・変更ファイル一覧を含めること

## 補足(前回報告への返答)

「想定外の--amendコミット」の件、force pushなしで整理した判断でOK。
今後も履歴の分岐を見つけたら同様に安全側で処理し、報告に含めること。
