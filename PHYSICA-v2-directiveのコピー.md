# PHYSICA v2 開発指示書

v1(デプロイ済み・検品済み)への機能追加。まずこの指示書から **v2仕様書(PHYSICA-spec-v2.md)を書き起こし、実装計画を提案して承認を得てから** 実装に入ること。

## 追加機能1: 目標システム(3層+期間目標)

ホーム画面に目標カードを縦に並べる(締切が近い順、上=短期)。

### 目標の型(4種)。型+データで抽象化し、後から型を追加できる構造にする

```json
{ "type": "exam" | "mocktest" | "rank" | "period",
  "label": "...", "deadline": "YYYY-MM-DD", "target": { 型ごとの定義 } }
```

- **exam(定期考査/短期)**: 既存の定期考査モードを目標カードに統合。
  範囲(章選択)+試験日。表示=予想得点・カウントダウン
- **mocktest(模試/中期)**: exams.json から模試をプルダウン選択。
  日付はデフォルト値入りで微調整可(学校実施日ズレ対応)。
  範囲=章選択(考査と共通UI)。
  表示=「仕上がり度%」(予想得点とは呼ばない。初見問題への予想を装わない)
  +範囲限定レーダー+カウントダウン
- **rank(志望ランク/長期)**: 既存のランク判定を目標カード化。志望を1つ選択。
  表示=現ランク・志望まで残り点・弱点分野
- **period(期間目標)**: 「夏休みに力学2周」型。期間+獲得目標点(または範囲×周回数)。
  進捗=**期間中の獲得量(減衰無視の生タップ加点合計)**。
  表示=積み上げ進捗バー・「残りN日であとM点」

### exams.json(模試マスタ、年度ごと差し替え)

```json
{ "id": "zento-k2-kijutsu", "主催": "河合塾", "名称": "全統記述高2模試",
  "対象学年": 2, "標準時期": "2027-02", "科目": ["物理"] }
```

- config.json に "subject": "物理" を追加。プルダウンには
  対象学年が一致し科目に subject を含む模試のみ表示(他教科展開の余白)
- 2026年度の河合塾(全統)・駿台・ベネッセ進研・東進の模試を各社公式サイトで
  調査して収録。**物理(理科)を含む回のみ**。高1模試は英数国のみで理科が
  ないものが多い点に注意し、科目構成を必ず確認すること

## 追加機能2: レーダーチャートの軸変更

- 軸を予想得点から**分野別カバー率**(一度でも解いた問題の割合、減衰しない)に変更
- 各軸ラベルに予想得点を数値併記
- 理由: 現状は分母が重く序盤に針が動かずやる気が出ない。カバー率は
  1問解けば必ず動き、絶対に下がらない

## 追加機能3: タイルの周回表示(案A)

- **色相=周回数**: 1〜5周で色相が進み、6周目以降は最上位色固定
- **濃淡=保持率**: 各色相内で現行の減衰表現を維持
- **周回数バッジ**: タイル隅に「×2」等を常時表示(1周目は非表示)。
  色覚多様性への配慮として数字併記は必須
- 色割り当ては config.json に外出し
  (デフォルト: 1周=緑, 2周=青, 3周=紫, 4周=ピンク, 5周〜=金)
- 凡例を周回色相対応に更新

## 制約・作法

- v1の思想を維持: 履歴のみ保存・都度計算 / 全パラメータconfig外出し /
  相対パス / Vanilla JS / フラットデザイン
- **sw.jsのキャッシュバージョンを必ず上げる**
- 完了後: push → 本番URLでヘッドレス検品(全アセット200・SW更新・オフライン起動・
  新機能の動作) → ヘッドフルで375px幅のUI検品(目標カード3枚以上並べた状態、
  周回色相とバッジ、カバー率レーダー) → スクリーンショット付きで報告

## 判断に迷ったら

実装前に質問すること。特に「仕上がり度%」の算出式(予想得点と同式か、
係数を別に持つか)は仕様書段階で案を出して確認を取ること。

## 追加機能4: 勉強パッケージ(period型のプリセット+日割りエンジン)

生徒は勉強計画を立てられない前提で、選ぶだけで日々のノルマまで落ちる仕組み。

### packages.json(パッケージマスタ、管理者=清水が編集して追加していく)

```json
{ "id": "summer-full-1", "名称": "夏休みで全範囲1周",
  "範囲": { "章": "all" }, "周回": 1, "推奨期間日数": 40,
  "対象学年": 3, "コメント": "受験の土台づくり" }
```

範囲は章の配列 or "all" or 分野名で指定可能にする。

### 動作

1. パッケージを選ぶ→開始日(今日)と終了日(推奨日数から自動、変更可)で
   period型の目標として登録される
2. **日割りエンジン**: 残りノルマ(タップすべき残問題数)÷残日数を毎日
   引き直し、ホーム最上部に「今日: あと N問」を表示。
   遅れは自動的に翌日以降へ再配分される(特別な繰越処理は作らない。
   毎日の割り算で自然に増える)
3. **今日の問題指名**: 範囲内で保持率が低い順にN問を「今日の問題」として
   リスト提示。タップで通常どおり加点され、ノルマが減る
4. パッケージ完了(範囲×周回ぶんのタップ達成)で完了演出

### 備考

- packages.json の編集だけで新パッケージを追加できること(コード変更不要)
- v1同梱パッケージは仮で2つ: 「夏休みで全範囲1周(40日)」
  「2週間で波動1周(14日)」。中身は後で管理者が拡充する

## 追加機能5: curated型パッケージ(監修型ペースメーカー)

パッケージに第2の型を追加する。auto型(日割り自動)に対し、curated型は
**日ごとの問題リストを管理者が指定**する。実物サンプルとして
「2026予定表.pdf」(2025夏 物理ペースメーカー)を同梱するので、これを
初回データとして変換すること。

### データ構造

```json
{ "id": "summer-k3", "型": "curated", "名称": "夏のペースメーカー",
  "days": [
    { "day": 1, "リード章": 1,
      "問題": ["1-基本問題15", "1-基本問題18", ...],
      "重問": [...],
      "テーマ": "力学 — 物理の基本。焦らずに全てを身につけよう",
      "動画URL": null },
    ...
  ] }
```

- 日付は絶対日付でなく開始日からの相対(day: n)。開始時に実日付へ展開
  (来年度の使い回し・途中参加に対応)
- 「復習と調整」の日は問題指定なしの調整日として表現
- 動画URLフィールドは現状nullで確保のみ(将来のYouTube連携用)

### PDFからの変換ルール

- リードαの問題番号・基本例題はPDF記載のとおり変換
- **重問はPDFの番号(2023年版)を使わない**。その日のリードα章と同じ範囲に
  該当する2026年版(juyomon-mapping.csv)の問題を、分野・進度に沿って
  割り当て直す。章→重問の対応と日ごとの配分は変換時に提案し、
  一覧表にして報告(管理者が検品する)
- 『理論物理への道標』『授業プリント』の行は変換対象外(アプリに含めない)

### 表示

- curated型を開始すると、ホーム最上部に「今日の問題」として当日リストを表示。
  タップで通常加点、消化率がパッケージ進捗になる
- 遅れた場合: 未消化の問題は「たまり」として当日リストの下に別枠表示
  (auto型のような自動再配分はしない。監修どおりの進度を正とする)
