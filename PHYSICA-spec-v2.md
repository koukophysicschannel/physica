# PHYSICA 仕様書 v2(差分)

本書は `PHYSICA-spec.md`(v1)への追加分のみを記す。v1に明記済みの技術方針(Vanilla JS・
ビルド工程なし・localStorageのみ・相対パス・履歴のみ保存/都度計算・全パラメータconfig外出し)は
すべてそのまま維持する。

> 実装前レビューで生じた設計判断(仕上がり度%の式・ナビ改修・日割り適用範囲・
> ノルマ頭打ち)はすべて確定済み。以下は確定版。

---

## 0. データモデルの根本変更: 単一「定期考査モード」→ 複数「目標」

v1は `physica_exam_settings_v1` に**単一の**定期考査設定のみを保存していた。v2ではこれを
**複数の目標(ゴール)を保持する配列**に置き換える。

### storage: `physica_goals_v1`

```json
[
  {
    "id": "goal-uuid-or-timestamp",
    "type": "exam" | "mocktest" | "rank" | "period",
    "label": "第2回定期考査",
    "deadline": "YYYY-MM-DD",
    "createdAt": 1732000000000,
    "target": { /* 型ごとに異なる。下記参照 */ }
  }
]
```

- ホーム画面には目標カードを `deadline` 昇順(締切が近い順、上=短期)で縦に並べる。
- v1の `physica_exam_settings_v1` からの移行: 起動時に旧キーが存在すればワンショットで
  `type: "exam"` の目標に変換して `physica_goals_v1` に追記し、旧キーは削除する
  (データ消失を避けるための移行処理。恒久的な互換レイヤーにはしない)。

### 型ごとの `target`

| type | target の中身 | 進捗/表示 |
|---|---|---|
| exam | `{ chapters: number[] }` | 予想得点(範囲=chapters)・カウントダウン。v1の定期考査モードと同一計算 |
| mocktest | `{ examId: string, chapters: number[] }` | 「仕上がり度%」(§2)・範囲限定レーダー・カウントダウン |
| rank | `{ rankLabel: string }` | 現ランク・志望ランクまでの残り点・弱点分野(v1のランク判定を流用) |
| period | `{ scope: Scope, rounds?: number, targetPoints?: number, packageId?: string }` | 積み上げ進捗バー・「残りN日であとM点」 |

`Scope` は3形式のいずれか: `{ "chapters": number[] }` / `{ "chapters": "all" }` / `{ "field": string }`。
リードα・重問の両方をカバー範囲に含める(パッケージ機能で「力学」のような分野指定を想定するため)。

- period の目標点は次のいずれかで決まる:
  - `targetPoints` が明示されていればそれを使う
  - 無ければ `rounds × (scope内満点)` を**目標作成時点で確定させ、`target.targetPoints` として固定保存**する
    (満点は問題データ変更で動きうるため、後から動かないように作成時に凍結する)
- 進捗計算: 目標の `createdAt` 以降に発生したタップのみを対象に、**減衰を無視した生の配点合計**を
  積算する(v1の「保持点」とは別の指標。既存の `computeHeldByProblem` とは別ルートで、
  タップのタイムスタンプ×配点を単純合計する関数を新設)。

### 目標の追加/編集/削除 UI

v1の「定期考査モード設定」画面(`examSetup.js`)を、4型すべてに対応する
**「目標を追加」フローを持つ目標管理画面**に置き換える。

**確定**: ボトムナビの「定期考査」タブを「目標」に改名し、4型共通の目標一覧・追加・削除を
行う画面にする(exam型の作成もここに統合。`examSetup.js` は `goals.js`(仮)に置き換わる)。

---

## 1. exams.json(模試マスタ)

```json
{
  "id": "zento-k2-kijutsu-2026-02",
  "主催": "河合塾",
  "名称": "全統記述高2模試",
  "対象学年": 2,
  "標準時期": "2027-02",
  "科目": ["物理"]
}
```

- 配列 `exams: [...]` をトップレベルに持つJSONとして `data/exams.json` に配置。
- `config.json` に `"subject": "物理"` を追加。
- mocktest目標作成のプルダウンには「対象学年が生徒の学年と一致し、かつ `科目` に
  `config.subject` を含む」模試のみ表示する。
  - 生徒の学年はどこで持つか: v1にその概念がない。**確定**: mocktest目標作成時に学年入力
    (1/2/3年)を一度だけ聞き、`localStorage`(`physica_profile_v1`)に保存して以後使い回す
    (学年設定画面などは新設せず、初回の目標作成フロー内で聞くのみに留める)。
- 日付初期値: `標準時期`(例 `"2027-02"`)は年月までしか分からないため、
  デフォルトで**その月の1日**を `deadline` の初期値とし、目標作成画面で日付ピッカーにより
  ユーザーが実施日に微調整できるようにする。
- 収録内容: 2026年度の河合塾(全統)・駿台・ベネッセ進研・東進について、各社公式サイトを
  調査し、**物理(理科)を含む回のみ**収録する(高1模試は英数国のみで理科がない回が多いため
  科目構成を個別に確認)。これは実装フェーズでの調査タスクとして実施する(仕様書段階では
  スキーマのみ確定させる)。

---

## 2. 「仕上がり度%」の算出式 — 確定

**確定**: 予想得点と全く同じ式(k=1.7)を流用し、呼び名だけ「仕上がり度%」に変える。

```
仕上がり度% = 範囲内保持点 / (範囲内満点 × 1.7) × 100    上限100
```

- 内部的には既存の `expectedScore(held, max, config.conversion)` をそのまま再利用でき、
  `config.json` に新規パラメータは追加しない。
- 画面表示上は「予想得点」ではなく必ず「仕上がり度%」というラベルを用い、
  数値の意味づけ(初見問題への予想ではない)は目標カード内の説明文で示す。

---

## 3. レーダーチャートの軸変更(カバー率)

- 軸の値 = **分野別カバー率** = (その分野で一度でも解いた問題数) ÷ (その分野の総問題数) × 100
  - 「一度でも解いた」= 対象範囲の問題のうち `taps.length > 0` の件数(配点ではなく**問題数**で数える)
  - 分野の対象範囲は既存の `statsByField` と同じ(config.fieldsに含まれる5分野、重問の「考察」は
    分野バランス計算から除外=v1と同じ扱い)
  - 一度でも解けば必ず動き、時間経過で下がることはない(減衰非依存)
- 軸ラベルには従来通り予想得点を併記: `"力学 78"`(78は予想得点の数値、四捨五入)。
  ただし針の位置(頂点の半径)はカバー率の値を使う点が変更点。
- `renderRadarSVG` のシグネチャを `(fields, coverageByField, expectedByField)` の2値渡しに変更。

---

## 4. タイルの周回表示(色相=周回数、濃淡=保持率)

### 色相の階層(config.jsonに外出し)

デフォルト5階層(v2指示書の記載どおり):

```json
"lapColors": {
  "1": "43,138,62",
  "2": "43,90,181",
  "3": "124,58,181",
  "4": "199,64,131",
  "5": "181,140,20"
}
```

- キー`"5"`は「5周以上」を表す(6周目以降も同じ最上位色で固定、という指示書の文言と
  デフォルト表の「5周〜=金」は同じ意味と解釈した→ タップ回数 `count` を
  `tier = Math.min(count, 5)` でクランプし、`lapColors[String(tier)]` のRGBを使う)
- 濃淡は現行どおり `lastTapRetention()` の値を alpha として `rgba(r,g,b,alpha)` に適用
  (未着手は無色のまま `tile-empty` を維持)

### 周回数バッジ

- タイル隅に `×N`(Nは実際のタップ回数、色相tierとは別)を常時表示。
  1周目(count === 1)のみ非表示(現行の `tile-count` 表示ロジックとほぼ同じだが、
  「1回目は隠す」条件を追加する必要がある — 現行は `count > 0` で表示している点に注意)。

### 凡例

- 現状「問題リスト」画面に色の意味を示す凡例UIは存在しない(新規追加)。
- タブの上か下に、5色のスウォッチ+ラベル(「1周目」「2周目」「3周目」「4周目」「5周目〜」)を
  横並びで表示する小さな凡例セクションを新設する。

---

## 5. 勉強パッケージ(period型のプリセット+日割りエンジン)

### packages.json

```json
{
  "packages": [
    {
      "id": "summer-full-1",
      "名称": "夏休みで全範囲1周",
      "範囲": { "章": "all" },
      "周回": 1,
      "推奨期間日数": 40,
      "対象学年": 3,
      "コメント": "受験の土台づくり"
    },
    {
      "id": "wave-2weeks-1",
      "名称": "2週間で波動1周",
      "範囲": { "分野": "波動" },
      "周回": 1,
      "推奨期間日数": 14,
      "対象学年": null,
      "コメント": ""
    }
  ]
}
```

- `範囲` は `{ "章": number[] }` / `{ "章": "all" }` / `{ "分野": string }` のいずれか
  (内部的には §0 の `Scope` 形式に変換して period目標の `target.scope` に格納)。
- `packages.json` を編集するだけで新パッケージが追加できること(コード変更不要)。
- `対象学年` でパッケージ一覧を絞り込み表示(nullは全学年向けとして常に表示)。

### 選択→目標作成

1. パッケージ選択画面(§0の「目標を追加」フロー内、type=period の作成時に
   「パッケージから選ぶ」/「自由に設定」の分岐を設ける)でパッケージをタップ
2. 開始日=当日固定、終了日=開始日+推奨期間日数(変更可能な日付ピッカー)
3. 確定すると period 型の目標としてv0のスキーマに従い保存される:
   `target = { scope, rounds, targetPoints: <作成時点のscope満点×rounds>, packageId }`

### 日割りエンジン(パッケージ由来のperiod目標にのみ適用)

**確定**: 日割り・今日の問題指名・完了演出は、**パッケージ経由で作成されたperiod目標
(`target.packageId`が存在するもの)にのみ**適用する。手動作成のperiod目標(§0, 追加機能1)は
積み上げ進捗バーのみのシンプル表示に留める。

- 「残りノルマ」の単位は**問題数**(点数ではない)。
  - 総ノルマ = `scope内の問題数 × rounds`
  - 達成済みユニット = `Σ min(その問題の期間内タップ数, rounds)`(**確定**: 同じ問題を
    必要以上に連打しても2周分・3周分の水増しにはならないよう、問題ごとに`rounds`で頭打ちにする)
  - 残りノルマ = 総ノルマ − 達成済みユニット
  - 残り日数 = `max(deadlineまでの日数, 1)`(締切当日・超過時のゼロ割回避)
  - 今日のノルマ = `Math.ceil(残りノルマ / 残り日数)`
  - 遅れが出た日は翌日以降このノルマ計算に自然に反映される(特別な繰越処理なし、
    指示書の意図どおり)
- ホーム最上部(全ての目標カードより上)に「今日: あとN問」を表示。
  複数のパッケージ目標が同時に走っている場合は目標ごとに1行ずつ表示する。

### 今日の問題指名

- 対象 = scope内かつ「その問題の期間内タップ数 < rounds」の問題(=まだそのパッケージの
  周回分をこなしていない問題)
- 保持率(`lastTapRetention`、未タップは0扱いで最優先)が低い順に並べ、上位「今日のノルマ」件を
  「今日の問題」としてタイルのリスト表示(タップの見た目・加点動作は問題リストのタイルと同じ
  コンポーネントを再利用する)
- タップで通常どおり加点され、達成済みユニットが増えて残りノルマが減る

### パッケージ完了

- 残りノルマが0以下になったら完了演出を表示する(絵文字は使わない、というv1のデザイン指針を
  維持しつつ、フラットデザインの範囲でカード全体の強調表示・完了メッセージ等で表現)

---

## 6. sw.js キャッシュバージョン

`CACHE_NAME` を `"physica-v2"` に上げ、`PRECACHE_URLS` に新規追加ファイル
(`data/exams.json`, `data/packages.json`, 新設するビューJSファイル群)を追加する。

---

## 設計判断まとめ(確定済み)

1. **「仕上がり度%」の算出式**(§2): 予想得点と同じ式(k=1.7)を流用、呼び名のみ変更。
2. **ナビゲーション改修**(§0): 「定期考査」タブを「目標」タブに改名し、4型共通の目標管理画面に
   置き換える。
3. **日割りエンジンの適用範囲**(§5): パッケージ経由のperiod目標にのみ適用。手動period目標は
   進捗バーのみ。
4. **達成済みユニットの頭打ち**(§5): `min(期間内タップ数, rounds)` で問題ごとに頭打ち。
5. **生徒の学年の保持**(§1): mocktest目標作成時に一度だけ聞き `physica_profile_v1` に保存。
