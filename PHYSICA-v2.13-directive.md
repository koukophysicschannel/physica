# PHYSICA v2.13 開発指示書

2件。完了したらsw.jsバージョンを上げて本番反映・検品まで。報告には
バージョン・commit ID・変更ファイル一覧。HANDOFF.mdの運用ルールに従うこと。

## 1. 問題評価システム(しみまさ査定)

### データ

- data/ratings.json を新設。形式: { "問題ID": { "stars": 1|2|3, "skip": true|false } }
- 未評価の問題はデフォルト扱い(stars: 2、skip: false)
- ratings.jsonはビルド生成物ではなく管理者が育てるマスタデータ。
  build_problems.pyでproblems.jsonに評価をマージする

### 評価入力ページ(admin.html)

- ナビからリンクしない隠しページ(URL直打ちでのみ到達)
- 問題を1問ずつ表示(章・種別・番号・問題名)し、大きなボタンで
  ★1/★2/★3、スキップ可トグル、を付けて自動で次の問題へ
- 進捗表示(評価済み/全問題数)、章・範囲の絞り込み、中断・再開可能
- 評価はlocalStorageに保存し、「ratings.jsonをエクスポート」ボタンで
  ダウンロード。既存のratings.jsonをインポートして続きから評価も可能
- スマホ(375px)での片手操作を最優先に設計(通勤中の評価作業を想定)

### アプリへの反映

- タイルに★表示(小さく。★2は表示なし=デフォルト、★3のみ強調、
  ★1は控えめ表示、スキップ可はタイルを淡く)
- 「今日の問題」の選問ロジック: ★3を優先、スキップ可を除外
- **配点には反映しない**(満点・Tier較正を崩さないため)
- about.htmlに★の意味を1段落追記

## 2. curated型パッケージ(未実装の積み残し)

- v2指示書の「追加機能5」(curated型・監修型ペースメーカー)を実装する
- 元データ「2026予定表.pdf」をプロジェクトフォルダに配置済み(要確認。
  なければ着手前に報告)
- 重問の割り当てはbuild/juyomon-lead-map.jsonを使用。確信度「低」の
  対応は仮割り当てとしてフラグを付け、レビューCSVで検品可能にする
- 実装仕様はv2指示書の該当セクション(データ構造・日割り・遅れは
  「たまり」方式)に従う

## 検品

- admin.htmlでの評価→エクスポート→ビルド→タイル反映の一連の流れ
- curated型: パッケージ開始→今日の問題表示→タップで消化→たまり表示
- 本番: 全アセット200・SW更新・オフライン起動・375pxスクリーンショット
